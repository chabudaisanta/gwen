---
title: std::vector入出力 (vector.hpp)
documentation_of: //include/gwen/io/vector.hpp
---

# std::vector入出力

`std::vector` に対する `std::istream` および `std::ostream` のオーバーロードを提供します。

## 概要
- `std::cin >> v;` でベクター全体に順に値を読み込みます。
- `std::cout << v;` でベクターの要素を空白区切りで出力します。
